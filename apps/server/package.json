{
  "name": "@opencouncil/server",
  "version": "0.3.0",
  "private": true,
  "description": "OpenCouncil server + CLI. Built and published as part of the root `opencouncil` package; this manifest ships alongside dist/ so Node treats the bundle as ESM.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "npm --prefix ../.. run build:server",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
