/** Member + council CRUD routes. */
import type { FastifyInstance } from 'fastify';
import type { DB } from '../db/connection.js';
export declare function registerMemberCouncilRoutes(app: FastifyInstance, db: DB): void;
